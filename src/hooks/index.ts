// =============================================================================
// Hooks — Custom React hooks barrel export
// =============================================================================
// TanStack Query wrappers, debounce, pagination, and other reusable hooks.
// =============================================================================

export { usePagination } from './use-pagination';
export { useDebounce } from './use-debounce';
export { usePermissions, useRequireAuth } from './use-auth';

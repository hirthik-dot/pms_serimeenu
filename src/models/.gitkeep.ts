// =============================================================================
// Models — Mongoose schema definitions placeholder
// =============================================================================
// Each model file defines a Mongoose schema, indexes, virtuals, and hooks.
// Models are ONLY imported by repositories — never by services or route handlers.
// =============================================================================
